PORT=
FRONT_URL=
MONGO_URI=
SHYFT_API_KEY=
RPC_URL=
WSS_URL=
CMC_API_KEY=
HELIUS_API_KEY=
ALPHA_WHALES_X_BEARER_TOKEN=
ALPHA_WHALES_X_API_KEY=
ALPHA_WHALES_X_API_KEY_SECRET=
ALPHA_WHALES_X_ACCESS_TOKEN=
ALPHA_WHALES_X_ACCESS_TOKEN_SECRET=
ALPHA_WHALES_X_CLIENT_ID=
ALPHA_WHALES_X_CLIENT_SECRET=
ALPHAX_AGENT_X_BEARER_TOKEN=
ALPHAX_AGENT_X_API_KEY=
ALPHAX_AGENT_X_API_KEY_SECRET=
ALPHAX_AGENT_X_ACCESS_TOKEN=
ALPHAX_AGENT_X_ACCESS_TOKEN_SECRET=
ALPHAX_AGENT_X_CLIENT_ID=
ALPHAX_AGENT_X_CLIENT_SECRET=
ALPHA_BOT_TOKEN=
CHANNEL_CHAT_ID=
BIRD_EYE_API_KEY=
# Set to true/1 to disable BirdEye cron jobs (saves credits). Top Coins cache + Trending list will stop updating.
# DISABLE_BIRD_EYE_CRONS=true
SERVER_URL=

# Redis (required for BullMQ / whale & KOL queues). If your Redis requires auth, set REDIS_PASSWORD or REDIS_URL.
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# Or use a single URL (password in URL): REDIS_URL=redis://:yourpassword@host:6379

# Jupiter Ultra API Configuration (v1)
# Base URL: https://api.jup.ag/ultra/v1/
# Endpoints: /order (quote+swap), /execute, /search, /holdings, /shield
# Get your API key at: https://portal.jup.ag
JUPITER_BASE_URL=https://api.jup.ag/ultra/v1
JUPITER_ULTRA_URL=https://api.jup.ag/ultra/v1
JUPITER_API_KEY=your-jupiter-api-key-here
JUPITER_REFERRAL_KEY=your-referral-account-public-key-here
JUPITER_DEFAULT_PRIORITY=High
JUPITER_ENABLE_DYNAMIC_SLIPPAGE=true

# Parser Selection: set to "true" to use Helius-native V3 parser instead of SHYFT V2
USE_HELIUS_PARSER=false
# Shadow mode: run BOTH parsers and log comparison (only when SHYFT is main). Keep false when USE_HELIUS_PARSER=true.
HELIUS_SHADOW_MODE=false

# Whale monitor: use batched WebSocket subscriptions when address count > this (default 500). Helps get more txs when tracking 4000+ whales.
# WHALE_BATCH_SUBSCRIBE_THRESHOLD=500
# Whale subscription batch size when using batches (default 100 → 40 batches for 4000 addresses).
# WHALE_SUBSCRIPTION_BATCH_SIZE=100
# Whale workers: increase if whale tx volume is low (e.g. NUM_WORKERS_WHALE=2, WORKER_CONCURRENCY_WHALE=12, RATE_LIMIT_MAX_WHALE=60)